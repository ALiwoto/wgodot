R"(
## Implemented by classes that can serialize themselves into a compact binary representation.
interface_name BinarySerializable

## Returns this object encoded as binary data.
func serialize_binary() -> PackedByteArray
)"
